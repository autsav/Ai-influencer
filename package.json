{
  "name": "aeloria-fal-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Modular fal.ai payload router for the Aeloria image pipeline (standalone TS subtree)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
